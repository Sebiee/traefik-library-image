name: Build and Push Docker Images

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to build (e.g., v3.7.0)'
        required: true
        type: string

env:
  GHCR_REGISTRY: ghcr.io
  DOCKERHUB_IMAGE: sebiee/traefik
  GHCR_IMAGE: ghcr.io/sebiee/traefik

jobs:
  build-alpine:
    name: Build Alpine Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        platform:
          - linux/amd64
          - linux/arm64

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV
          else
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Prepare version directory
        run: |
          VERSION_DIR="${VERSION/%.+([0-9a-z\-])/}"
          echo "VERSION_DIR=${VERSION_DIR}" >> $GITHUB_ENV
          ./updatev3.sh "${VERSION}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract platform name for tag
        id: platform
        run: |
          PLATFORM="${{ matrix.platform }}"
          PLATFORM_TAG="${PLATFORM//\//-}"
          echo "tag=${PLATFORM_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push Alpine image
        uses: docker/build-push-action@v6
        with:
          context: ./${{ env.VERSION_DIR }}/alpine
          file: ./${{ env.VERSION_DIR }}/alpine/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-alpine-${{ steps.platform.outputs.tag }}
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-alpine-${{ steps.platform.outputs.tag }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.version=${{ env.VERSION }}

  create-alpine-manifest:
    name: Create Alpine Multi-arch Manifest
    runs-on: ubuntu-latest
    needs: build-alpine
    permissions:
      contents: read
      packages: write

    steps:
      - name: Set version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV
          else
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push Alpine manifest (GHCR)
        run: |
          docker manifest create ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-alpine \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-alpine-linux-amd64 \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-alpine-linux-arm64
          docker manifest push ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-alpine

      - name: Create and push Alpine manifest (Docker Hub)
        run: |
          docker manifest create ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-alpine \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-alpine-linux-amd64 \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-alpine-linux-arm64
          docker manifest push ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-alpine

  build-scratch:
    name: Build Scratch Images
    runs-on: ubuntu-latest
    needs: create-alpine-manifest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        platform:
          - linux/amd64
          - linux/arm64

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV
          else
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Prepare version directory
        run: |
          VERSION_DIR="${VERSION/%.+([0-9a-z\-])/}"
          echo "VERSION_DIR=${VERSION_DIR}" >> $GITHUB_ENV
          ./updatev3.sh "${VERSION}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract platform name for tag
        id: platform
        run: |
          PLATFORM="${{ matrix.platform }}"
          PLATFORM_TAG="${PLATFORM//\//-}"
          echo "tag=${PLATFORM_TAG}" >> $GITHUB_OUTPUT

      - name: Build and push Scratch image
        uses: docker/build-push-action@v6
        with:
          context: ./${{ env.VERSION_DIR }}/scratch
          file: ./${{ env.VERSION_DIR }}/scratch/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-${{ steps.platform.outputs.tag }}
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-${{ steps.platform.outputs.tag }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.version=${{ env.VERSION }}

  create-final-manifests:
    name: Create Final Multi-arch Manifests
    runs-on: ubuntu-latest
    needs: build-scratch
    permissions:
      contents: read
      packages: write

    steps:
      - name: Set version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV
          else
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push version manifest (GHCR)
        run: |
          docker manifest create ${{ env.GHCR_IMAGE }}:${{ env.VERSION }} \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-linux-amd64 \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-linux-arm64
          docker manifest push ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}

      - name: Create and push version manifest (Docker Hub)
        run: |
          docker manifest create ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }} \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-linux-amd64 \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-linux-arm64
          docker manifest push ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}

      - name: Create and push latest manifest (GHCR)
        run: |
          docker manifest create ${{ env.GHCR_IMAGE }}:latest \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-linux-amd64 \
            ${{ env.GHCR_IMAGE }}:${{ env.VERSION }}-linux-arm64
          docker manifest push ${{ env.GHCR_IMAGE }}:latest

      - name: Create and push latest manifest (Docker Hub)
        run: |
          docker manifest create ${{ env.DOCKERHUB_IMAGE }}:latest \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-linux-amd64 \
            ${{ env.DOCKERHUB_IMAGE }}:${{ env.VERSION }}-linux-arm64
          docker manifest push ${{ env.DOCKERHUB_IMAGE }}:latest

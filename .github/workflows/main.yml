name: Main

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  # Tests disabled for fork - we build custom versions that don't exist in Docker Hub yet
  # The build-and-push workflow handles the actual building and testing
  placeholder:
    name: Placeholder (tests disabled for fork)
    runs-on: ubuntu-latest
    steps:
      - name: Skip tests
        run: echo "Tests skipped - custom fork builds are tested during build-and-push workflow"
